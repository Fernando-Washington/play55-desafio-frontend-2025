module.exports = {
  darkMode: ["class"],
  theme: {
    extend: {
      colors: {
        bodyBg: "var(--body-bg)",
        cardBg: "var(--card-bg)",
        inputBg: "var(--input-bg)",
        textColor: "var(--text-color)",
        accentColor: "var(--color-accent)",
      },
    },
  },
};
